# Nyhavna-demoen – status og overlevering 2026-09-13 kl. 11:10

Skrevet fordi Claude-kvoten er brukt opp; Astra (eller en ny sesjon) kan fortsette herfra. Alt under er committet lokalt på `main` (`3b9319d`), ikke pushet. Ingen filer er halvveis redigert.

## Tilstand nå

- Demo: `http://127.0.0.1:3101/eiendom/nyhavna-utvikling/nyhavna/leve` kjører det siste bygget (`npm run demo:nyhavna`, mini + marin). Alle chunks 200, verifisert på 1440×900 og 390×844 med reload uten cache.
- Mekanisk: tsc 0 feil, lint 0 feil, 260 filer / 4 212 tester grønne, produksjonsbygg OK.
- Worklog: siste innlegg i `PROJECT-LOG.md` («demoen stabil, ventetiden målt runde for runde, takgrensen funnet»). Runbook og `lyttetest.md` oppdatert. Trello «Utvikling» kort 30 har kommentar med neste handling.

## Hva som er funnet og rettet (kort)

- Tom side/HTTP 500 på chunk: bygg kjørt mens 3101 kjørte. Regel: stopp 3101 før `npm run build`.
- Ventetid: målt per tur med logglinjen `nyhavna_realtime_turn` (i `lib/realtime/sideband.ts`). Uten takgrense: første ord 0,6–2,5 s, kartendring 1,4–4,8 s. «30–40 s» = OpenAI-takgrensen **40 000 tokens/min for gpt-realtime-2.1-mini** på organisasjonen; én modellrunde koster 8 000–16 000 inndata-tokens, bufrede tokens teller fullt. Fra tredje–fjerde tur i rask rekkefølge venter brukeren 10–45 s eller får ikke svar.
- Rettet: `show_place`/`highlight_places` der mini bare innledet og så tiet (fakta/rekkefølge følger nå kartsvaret og modellen får ordet, `spokenAnswers()` i `nyhavna-conversation.ts`); tema-/stedstrykk forhåndsutføres på serveren (`interceptUserMessage` → `onUserText`), én runde spart; Nyhavnas egne temaer først i `set_interests`; kapitler uten dobbel FAQ; prosjektinnhold én gang; kortere verktøytekster; tre forsøk ved takgrense.
- Verifisert live: ulike interesser → ulikt første tema; tre fremhevede med tall; «det andre stedet» riktig 3/3; temaklikk → kapittel + markører på 1,5–1,9 s; retur; pris → ærlig «ikke i materialet»; Stopp rydder; hilsen ordrett.

## Åpent (i prioritert rekkefølge)

1. **Andreas: hev OpenAI-tier før 16.09** (Settings → Limits). Uten det nås ikke fem-sekundersmålet pålitelig – dette er den største spaken.
2. **Stemme ikke vurdert.** Opptak av hele siste samtale (7 min, variant A, mini + marin) i `.context/nyhavna-lytteprove-2026-09-13.m4a` (ikke i git). Effektiv konfigurasjon bekreftet. Transkripsjonen viser slang («Konge», «Kjempegrei», «Perfekt») – vurder å legge «ingen slang, ingen fyllord som innledning» i `NYHAVNA_INSTRUCTIONS` (SVARLENGDE/UTTALE). Protokoll: `lyttetest.md`.
3. **Ikke verifisert live:** avbrudd midt i tale (avbruddet traff en takgrense-pause), endrede interesser (bare enhetstest).
4. **Neste kodegrep, designet men ikke bygd** – fjerner én modellrunde per tema og gir kart på ~1,5 s:
   - Server fremhever kapittelets 3 første steder selv når et tema åpnes (`set_interests`, `open_theme`, temaklikk): sideband-et sender et systemelement `Placy-kart: {"name":"highlight_places","args":{"poi_ids":[...]}}` + én setning til modellen («Kartet fremhever nå 1 A; 2 B; 3 C – omtal dem i denne rekkefølgen, ikke kall highlight_places for disse»).
   - Klienten (`use-realtime.ts`, `conversation.item.*`-casen) kjenner igjen prefikset, kjører `executeTool` (samme validering/kamera som i dag) og svarer med et systemelement `Placy-kartstatus: {...}` som sideband-et gir til `observe`.
   - Serveren oppdaterer `TourState.highlighted` optimistisk med ID-er den selv har validert mot boardet, så notatet er riktig før modellen svarer; browser-svaret bekrefter. `tour-state.ts`: `highlight` med samme rekkefølge skal ikke bumpe revisjon.
   - Nye konstanter i `lib/realtime/types.ts` (`MAP_DIRECTIVE_PREFIX`, `MAP_RESULT_PREFIX`), ny `SidebandOptions.directive`, ny `conversation.takeMapDirective()`. Tester i `sideband.test.ts`, `nyhavna-conversation.test.ts`, `use-realtime.test.tsx` (mønster: «Realtime early map execution»).
   - Effekt: interesse-tur 3 → 2 runder, temaklikk 2 → 1 runde. Med tier-heving i tillegg bør fem sekunder holde.
5. Korte kart-ID-er i stedet for UUID/Google-ID (≈ 18 % av katalogens tegn, mer i tokens) – lavere prioritet.
6. Maskinell språksjekk av opptaket (transkriber klippene uten språkhint, se etter engelske ord) – billig proxy, ikke en dialektvurdering.

## Slik kjører du en simulert samtale

`?voicedev=1` → `window.placyVoice` (`start()`, `say()`, `tool()`, `messages()`, `status()`, `stop()`). Les objektet på nytt per avlesning. Chromes falske mikrofon sender en tone som VAD tolker som tale – overstyr `navigator.mediaDevices.getUserMedia` med en stille strøm før `start()`. Tidsmålingene står i serverloggen (`nyhavna_realtime_turn`), kostnad i `nyhavna_realtime_usage` (siste kjøring: 26 svar, ≈ $0,10).
