# Helhetlig pre-beads-audit — 2026-06-28

> **Status: GJENNOMFØRT + REMEDIERT (autonomt, Andreas ikke til stede).**
> Siste gate før beads-ifisering. Resultat: **Beads-dom GO** etter én remediering-runde.
> Kilde-data: workflow `wf_77972a51` (audit) + `wf_429ee47b` (remediering). Kanonisk banking: `00-INDEX.md` note #14 + status-blokk.

## Hva ble gjort

| Fase | Metode | Agenter |
|------|--------|---------|
| 1. Audit | 15 per-PRD-auditører (hver mot ratifisert canon: 00-INDEX note #1–#13) → 1 synteseleder (kryss-PRD-tråder + GO/NO-GO) | 16 |
| 2. Remediering | edit→verify-pipeline, én editor + én uavhengig verifikator per PRD, pre-resolverte handlingslister | 30 |
| 3. Sluttføring | Manuell konsolidering: PRD 9/1-residualer, 00-INDEX-banking (note #14), grep-sveip på tvers av alle 15 | — |

Ingen edits pushet (prototype-regel). Alt banket lokalt.

## Synteselederens dom: NO-GO → GO etter remediering

Dommen var **NO-GO**, men eksplisitt «marginalt og kurerbart»: 10/15 PRD-er var allerede beads-klare, grafen strukturelt sunn (units ≤8, fasinndeling ikke P0/P1/P2, ACs kode-forankret, 10←14 lag-back-edge kjent+løst). **Alle blokkere var kontrakt-/dokumentasjons-korreksjoner av allerede-ratifiserte beslutninger — 0 arkitektur-redesign, 0 feature-scope-endring.**

### Blokkere remediert (6 + 1 build-blokker)

| # | PRD-er | Problem | Fiks |
|---|--------|---------|------|
| 1 | 7×12 | Dobbel-build av `app/api/admin/revalidate/route.ts` (to units, ulik auth-gate) | Fjernet fra PRD 7 (G8/§4/§6/Unit 8/Fase 3); PRD 12 eier (requireAdmin()). PRD 7 beholder kun secret-gated `/api/revalidate` |
| 2 | 3×4 | Unit 3 AC6 mandaterte `fetchAndCachePOIPhotos` (foto) som PRD 4 deferrer i samme fil | Foto-leddet markert DEFERRED i PRD 3; AC6 reframet til no-photo-fallback |
| 3 | 3 (+1,12) | `intern` no-customer-fallback helt fraværende | AC lagt i Unit 5/8: ingen kunde → `intern` → `intern_<slug>` (bevarer `{customer}_{slug}`-invariant) |
| 4 | 5×9 | Dependency-cycle 5→`getProjectBrokers`(9)→5 | Reframet: I/O-fri in-memory type-kontrakt, hoistbar til delt Lag-2 (som beat-signal-noden) — per-unit consume-edge, ikke board-blokker. INDEX-deps ←01,02,03 står |
| 5 | 9×2 | `TIER_CAPABILITIES`-symbol + validator-konsum av camera-tours/brokers/brand siterte SLETTEDE PRD 2-symboler | Symbol + `requires*`/`recommends*`-feltliste slettet; reframet til PRD 2s kondisjonell-render-kontrakt; `getCameraTour` → PRD 3-provisjon-kontrakt |
| 6 | 14×5×1 | `BoardCategoryId`-eierskaps-motsigelse (PRD 14 hevdet flytte-eierskap, PRD 5 beholdt in-place) | Pinnet: PRD 5 EIER type-hjemmet (`lib/board/board-types.ts` + re-eksport); PRD 14 KONSUMERER |
| 7 | 3/4/5/8/13 | v2-schema-targeting ikke propagert til ACs (build-blokker for PRD 13) | `.schema('v2')` / `Accept-Profile`/`Content-Profile: v2` banket i ACs; snapshot = kolonne-referanse; 11 = no-op (ingen DB-kall) |

### De fire deferred kryss-PRD-trådene — løst

- **(a) VO / nivå-3-validering (2/5/6/9/14):** VO = ortogonalt `pickPlayableAudio`-flagg (eid PRD 5, render-driver). PRD 2 sjekker IKKE VO. `TIER_CAPABILITIES`/`requiresPlayableVO`/`requires*`/`recommends*` slettet overalt (kun negative refs står). `getCameraTour`-signatur = SEPARAT load-bearing kontrakt mot PRD 3-PROVISJON, ikke nivå-3-validering.
- **(b) patch-#4 «delt foundation ≠ identisk overflate» (2/5/6/9):** ingen PRD forbød nivå-divergent UX — kun terminologi ryddet. Tråden var reelt allerede løst i grafen.
- **(c) 7↔8 editorial-lagring:** fysisk konsistent, ikke en motsigelse. PRD 7 → per-board fact-feed i `themes[].grounding`; PRD 8 → per-strøk editorial ÉN GANG i `areas.report_editorial`, arvet ned via `inherit-area-editorial.ts` → `themes[].editorial` (ren kopi, INGEN LLM ved arve-steg). Ortogonale JSONB-subnøkler, ingen skrive-kollisjon. «Én gang per strøk»-kilden = `areas.report_editorial`.
- **(d) foto-defer:** PRD 5 transform foto-agnostisk (nullable, AC lagt til); PRD 9 no-photo-fallback flyttet fra fotnote til faktiske ACs (Unit 2 AC7 / Unit 4 AC5 / Unit 8 AC5).

### Minor/nit ryddet (utvalg)

nivå-3-terminologi-sveip · INDEX-/PRD-1-linjeanker-drift re-ankret · «Claude-kuratering» → Fable (PRD 7/15) · `reportTier 1|2|3` → `1|2` (PRD 12) · establishing-shots-peker → PRD 9 (PRD 10) · travel-times reference-only (PRD 11) · `inheritAreaEditorial`-options-form pinnet (PRD 8) · `audioVersion` Zod→TS-literal (PRD 14) · `audioTourEnabled` presisert «dødt som playback-gate» (PRD 14) · category_id-payload-stabilitetsnote (PRD 13) · samlekart-spørsmål markert RESOLVERT (PRD 15) · poi_tier-disambiguering (PRD 4).

## Over-engineering-linse: REN

`found: false`. Den faktiske over-abstraksjonen (`TIER_CAPABILITIES` 3-rads matrise + 8-check-validator) ble allerede drept i walkthrough 27.06. Restene var stale pekere til det slettede systemet — cleanup-gjeld, ikke ny kompleksitet. Frikjent som legitim, bestilt foundation-grundighet: v2-schema-strategi, `intern`-fallback, to-fase trust-pipeline, beat-signal-/`BoardCategoryId`-hoist, event-payload-kontekst-konvolutt (anti-bloat: unngår for tidlig låsing).

## Utestående — venter på Andreas (IKKE autonomt avgjort)

- **Segment-adapter golden-output-test-trim (PRD 7 Unit 5).** Synteselederen anbefalte å nedgradere golden-output-regresjonstestene for `linkPOIsInText`, hvis eneste konsumenter er bekreftet-døde legacy scroll-komponenter som ikke bæres inn i rebuilden. Dette er en verifikasjons-effort-trim på død kode (berører IKKE feature-scope). **Ikke anvendt** — å redusere verifikasjons-scope er et eier-skjønn; over-testing av død kode er harmløst. Du avgjør.

## Beads-readiness: GO

15/15 PRD-er verifisert rene (grep-sveip: gjenværende «nivå-3»/`TIER_CAPABILITIES`-treff er alle negative/historiske). Units ≤8, deps eksplisitt + asyklisk (10←14 lag-back-edge løst som type/signatur-kontrakt), ACs konkrete. **Neste pipeline-stadium: beads dependency-graf → ralphy build-loop — ikke startet, venter på eksplisitt go.**
